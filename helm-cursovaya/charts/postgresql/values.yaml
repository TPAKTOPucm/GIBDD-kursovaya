replicaCount: 1

image:
  repository: postgres
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 5432

persistence:
  enabled: true
  size: 1Gi

env:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: ordersdb
